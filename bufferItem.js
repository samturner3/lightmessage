module.exports = function BufferItem(x, y, text, font, r, g, b, id) {
  this.x = x;
  this.y = y;
  this.text = text;
  this.font = font;
  this.r = r;
  this.g = g;
  this.b = b;
  this.id = id;
};

// drawText (x, y, text, font, r, g, b)
